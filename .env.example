# These variables are optional and for future use with models requiring an API key.
# The current application uses local models and does not require a .env file to run.
HUGGINGFACE_API_KEY="your_hugging_face_api_key_here"
HUGGINGFACE_MODEL="repository_id_of_a_model"